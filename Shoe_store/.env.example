# Frontend environment variables
# Currently the app calls "/api" for backend requests.
# In production, configure your web server (e.g., Nginx/Netlify/Cloudflare) to proxy
# /api to your backend, or refactor to use VITE_API_BASE and update fetch calls.
# Example if you choose to refactor:
# VITE_API_BASE=https://api.example.com
